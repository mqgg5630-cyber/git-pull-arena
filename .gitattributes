# Office 文档按二进制处理，避免 git 尝试做文本 diff / 行尾转换
*.docx  binary
*.doc   binary
*.pptx  binary
*.ppt   binary
*.xlsx  binary
*.xls   binary
*.pdf   binary

# 文本一律 LF 入库。core.autocrlf 会让 Windows 把签出的文本转成 CRLF，
# 于是同一个文件两边字节数不同（本机日志实测：VERSION 6→7 B、watch.ps1 83666→85361），
# 按字节判定的验收（min_bytes / sha256）就会假失败。
* text=auto eol=lf
*.sh    text eol=lf
*.py    text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.tsv   text eol=lf
*.yml   text eol=lf
VERSION text eol=lf

# 二进制大文件走 LFS（可选，需要本机安装 git-lfs；不装也不影响使用）
# *.docx filter=lfs diff=lfs merge=lfs -text
# *.pptx filter=lfs diff=lfs merge=lfs -text
