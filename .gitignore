# Office / WPS 临时文件
~$*
*.tmp

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store

# 构建产物
build/
dist/
*.log

# 会话中间产物（可重新生成）
build/

# pack.ps1 产出的交付压缩包（本地生成，不进 git）
_export/

# GitHub Actions 工作流：agent 的 GitHub App 令牌没有 workflows 权限，
# 任何包含 .github/workflows 的提交都会被远端拒绝（实测）。
# 需要 CI 闸门时由本机提交：
#   mkdir .github\workflows ; copy skills\git-sync\templates\gate.yml .github\workflows\gate.yml
#   .\push.ps1 "ci: enable the gate workflow"
.github/workflows/
