# 最近一轮同步回执（agent -> 分支）

- 时间：2026-09-16 00:22 UTC
- 分支：`arena/01a0a4f5-git-pull-arena`
- 本轮纳入的**本机侧**提交（本机 -> 助手 ✅）：
  - b98aaf8 check: request round 11 (awaiting local check)
  - d64ddd4 docs: 修正 CONNECTIONS 头部（v2.6.2
  - ab52919 fix: git-sync v2.6.2 —— 修正我自己的误报：auth.ps1 的 dry-run 推本地 HEAD，非快进被拒（failed to push some refs）被当成认证失败；改为推远端跟踪引用到一次性探针分支，被拒也计入 READY 并注明；push.ps1 在只领先结论提交时自动对齐远端后重试（修值守 exit 3 死循环）；local_check 2b 分级（零闪/每次登录闪一次/每轮闪）；-Headless 非管理员直接给命令；启动器冒烟失败打印 host 日志；doctor 提示清理 auto-stash；新增 deliverable/FIX_v2.6.2.md
  - 4c7821e check: request round 10 (awaiting local check)
  - 58da6d1 fix: git-sync v2.6.1 —— 真因=网络路径（gh 不走 git 的代理）：auth.ps1 自动套用 git config http.proxy + 新增 -HttpProxy + 网络不可达时的明确指引；PAT 入库改为离线优先（先写凭据助手，无需任何 API 调用）；sync.ps1 不再把值守产物堆成 stash（本地提交 + split 自动对齐远端）+ stash 堆积告警；watch.ps1 记录 push 失败原因 last_push_detail + 代理不一致提醒；push.ps1 认证措辞补全（unable to get password from user）+ 网络类失败指引；新增 deliverable/FIX_v2.6.1.md
  - 5bc7604 check: request round 9 (awaiting local check)
  - e00df26 feat: git-sync v2.6.0 —— 值守改常驻循环（一次登录一个进程：flash 模式也从每2分钟闪一次降到每次登录闪一次，启动器模式仍为零闪）+ 注册前启动器冒烟测试（失败打印任务结果与 host 日志）+ keeper 每30分钟保活 + -Status 报告循环存活/心跳年龄 + -Test 识别已运行；auth.ps1 新增 -GhLogin（一条命令完成唯一一次交互登录并 setup-git）+ 公开仓库 ls-remote 不代表鉴权的提示；push.ps1 -Prompt 显式开交互；doctor 认可 267009(RUNNING)；新增 deliverable/FIX_v2.6.0.md
  - 9e79d2b fix: push.ps1 认证失败识别补齐 GCM 实际措辞（interactivity has been disabled）+ 一次性登录的三种做法；FIX 文档补充闸门清单
  - 9246eea check: request round 8 (awaiting local check)
  - ca6688a fix: git-sync v2.5.1 —— 实测修复两个 bug：① watch.ps1 的 "$round:" 被当盘符变量导致整份脚本解析失败（改 ${round}: + gate 新增 scan_ps_var_colon.py 扫描器）② auth.ps1 -Setup 改为先探测/只在必要时动手（不再把 wincredman 里的凭据用 dpapi 藏掉，命中即 unset 回默认，另加 -MigrateStore）；配套：注册前环境预检（git/bash/pwsh 路径入心跳）、local_check 禁止 gate 空转通过、gate python3→python 回退、stderr 经 cmd 合并
  - 01beb09 fix: watch v2.5.0 —— 计划任务加 ExecutionTimeLimit（超时+10 分钟）+ fetch 低速超时：卡死的轮询不会永久占用任务实例（否则后续轮询被 IgnoreNew 静默挡掉）
  - 9e9ff2d docs: 补救弹窗主因 —— 摘掉搁置会话的本机值守（有多会话克隆时 3~4 个任务各闪一次窗）
  - e9f247a docs: 记录 v2.5.0 验收轮 round 7（检查项即两条硬要求：2a 免点击推送实跑 / 2b 零窗口启动器）
  - 9680c58 check: request round 7 (awaiting local check)
  - d42a555 feat: git-sync v2.5.0 —— 零弹窗值守（GUI 子系统启动器 + 注册自检 + -Status/-Test + 心跳）+ 免点击推送（auth.ps1：gh/GCM-dpapi + 关闭提示的实跑验证；push.ps1 默认静默 exit 4）+ 检查硬超时 + gate 解析检查 + 单会话优先（多会话搁置）
- 本轮助手提交：fix: git-sync v2.6.3 —— 修掉值守跑不完的最后三个坑：① sync.ps1 全部 git 调用改走 cmd /c（PS 5.1 在 EAP=Stop 下把子进程 stderr 当终止性错误，'Already on x' 曾打断整轮轮询）② check_cmd 解析为绝对路径并由 cmd /d /c 执行 + 超时杀进程树 + ANSI 读回修中文乱码 + Start-Process 失败退回进程内（修 %1 不是有效的 Win32 应用程序/exit 193）③ 产物提交 amend 化（实测堆积 313 个）；keeper 保活改 10 分钟；bash 优先用 git 自带的；新增 deliverable/FIX_v2.6.3.md
- 本轮改动文件：
   M CONNECTIONS.md
   M code/local_check.ps1
   M skills/git-sync/VERSION
   M skills/git-sync/scripts/sync.ps1
   M skills/git-sync/scripts/watch.ps1
   M skills/git-sync/templates/local_check.ps1
   M sync.ps1
   M watch.ps1
  ?? deliverable/FIX_v2.6.3.md

> 完整历史：`git log --oneline -10`；本机 `.\sync.ps1` 之后即可看到本文件。
